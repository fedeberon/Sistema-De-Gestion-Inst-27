CREATE TABLE `alumnos` (
  `ALU_ID` INT NOT NULL AUTO_INCREMENT,
  `ALU_NOMBRE` VARCHAR(45) NOT NULL,
  `ALU_APELLIDO` VARCHAR(45) NOT NULL,
  `ALU_DNI` VARCHAR(45) NOT NULL,
  `ALU_FECHA_NACIMIENTO` VARCHAR(45) NOT NULL,
  `ALU_CELULAR` VARCHAR(45) NOT NULL,
  PRIMARY KEY (`ALU_ID`));
